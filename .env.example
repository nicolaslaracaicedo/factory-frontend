# Backend URL base (server-side)
API_URL=http://localhost:3000

# Backend URL base disponible en cliente (si no se define,
# next.config.ts la tomara desde API_URL)
NEXT_PUBLIC_API_URL=http://localhost:3000
